<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <link rel="stylesheet" href="/styles.css" />
  <title>Profil - ENI Tickets</title>
</head>
<body>
  <h1>Profil utilisateur</h1>
  <p>Connecté en tant que <%= user.login %> (<%= user.role %>) | <a href="/logout">Déconnexion</a></p>
  <p><a href="/">Retour à la liste des tickets</a></p>

  <form method="post" action="/profile">
    <label>Login* : <input type="text" name="login" value="<%= user.login %>" /></label>
    <% if (errors.login) { %><span style="color:red;"><%= errors.login %></span><% } %><br/>

    <label>Nouveau mot de passe (laisser vide pour ne pas changer) : <input type="password" name="password" /></label>
    <% if (errors.password) { %><span style="color:red;"><%= errors.password %></span><% } %><br/>

    <button type="submit">Mettre à jour</button>
  </form>
</body>
</html>
